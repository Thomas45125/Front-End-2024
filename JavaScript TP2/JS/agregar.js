// Agregar un nuevo elemento al final del array
let nuevoElemento = Math.floor(Math.random() * 100) + 1;
array.push(nuevoElemento);

document.write (" Con un nuevo elemento al final: " + array)

console.log(" Con un nuevo elemento al final:" + array);// Mostrar el arreglo con el nuevo elemento al final en la pantalla